<%= form_with(model: egg_group) do |form| %>
  <% if egg_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(egg_group.errors.count, "error") %> prohibited this egg_group from being saved:</h2>

      <ul>
        <% egg_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
